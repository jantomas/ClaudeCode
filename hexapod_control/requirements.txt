# Core Python dependencies
# python-version>=3.11

# Azure IoT SDK
azure-iot-device==2.13.0
azure-iot-hub==2.6.1

# Computer Vision & AI
opencv-python==4.8.1.78
numpy==1.26.2
pillow==10.1.0

# Hailo SDK (install separately from Hailo repository)
# See: https://github.com/hailo-ai/hailo-rpi5-examples

# Sensor Interfacing
smbus2==0.4.3              # I2C communication
gpiozero==2.0.1            # GPIO control
pigpio==1.78               # Hardware PWM for servos (if using PCA9685 - legacy)
adafruit-circuitpython-bno055==5.4.10  # BNO055 IMU
adafruit-circuitpython-pca9685==3.4.9  # PCA9685 servo driver (legacy - optional)

# Scientific Computing
scipy==1.11.4              # IK solver utilities
ikpy==3.3.3                # Inverse kinematics library (alternative)

# Configuration & Logging
pyyaml==6.0.1
python-dotenv==1.0.0
loguru==0.7.2

# Communication
protobuf==4.25.1           # Efficient serialization
pyserial==3.5              # Serial communication (Pololu Maestro, GPS, LiDAR)
paho-mqtt==1.6.1           # MQTT client

# LoRaWAN (choose based on hardware)
# Option 1: For SX127x modules
# pyLoRa==0.4.0
# Option 2: For RAK modules
# rak811==0.8.1

# Data Storage
aiosqlite==0.19.0          # Async SQLite

# Async & Concurrency
asyncio-mqtt==0.16.2
aiofiles==23.2.1

# Utilities
dataclasses-json==0.6.3
python-dateutil==2.8.2

# Development & Testing
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.12.1
flake8==6.1.0
mypy==1.7.1

# Optional: ROS2 (if using ROS2 Humble)
# Install via: sudo apt install ros-humble-desktop python3-colcon-common-extensions
